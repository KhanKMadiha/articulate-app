# Copy to .env at repo root (next to package.json)
# Key from: https://console.anthropic.com/settings/keys — must start with sk-ant- and be the full string.
# One line, no spaces around =, no quotes:
ANTHROPIC_API_KEY=
